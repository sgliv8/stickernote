.wrapper_with_padding
	#notes.clearfix
		- unless @notes.blank?
			- @notes.each do |note|
				%a{href: (url_for [note])}
					.note
						%p.title= note.title
						%p.date= time_ago_in_words(note.created_at) + " ago"
		- else
			%h2 Add a note
			%p It appears you have not created any sticker note yet... Lets fix that. Why don't you start to create a new note right now!!!
			%button= link_to "New Note", new_note_path